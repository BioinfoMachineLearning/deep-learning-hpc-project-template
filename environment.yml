name: DLHPT
channels:
  - defaults
  - conda-forge
  - pytorch
dependencies:
  - python=3.8
  - anaconda
  - pytorch
  - torchvision
  - torchaudio
  - pip
  - pip:
      - -e .
      - -r file:requirements.txt
      - -r file:tests/requirements.txt